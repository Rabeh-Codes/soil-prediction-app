#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run lint-staged only on staged files
npx lint-staged

# Optional: Run full lint in background for continuous integration
(npm run lint:ci > .lint-report.log 2>&1 &)

